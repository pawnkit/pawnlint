main()
{
    Kick(playerid);
    x = 10;
    y++;
    z--;
    ++y;
    --z;
    foo(a, b, c);
    bar(x);
    stop timerid;
    return callcmd::goto(playerid, params);
    enum E_VALUES
    {
        E_VALUE
    }
}
