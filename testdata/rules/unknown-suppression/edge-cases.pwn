// pawnlint-disable-next-line discarded-expression -- keep this for now
a + 1;

main()
{
    // pawnlint-disable-next-line all
    b + 2;
    // pawnlint-enable discarded-expression
    c + 3;
    // pawnlint-disable-next-line bogus -- reason here
    d + 4;
}