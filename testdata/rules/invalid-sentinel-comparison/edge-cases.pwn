main()
{
    new INVALID_VEHICLE_ID = 5;
    new vehicleid = GetPlayerVehicleID(0);
    if (vehicleid == INVALID_VEHICLE_ID)
    {
    }
    if ((GetPlayerCameraTargetVehicle(0)) == INVALID_ACTOR_ID)
    {
    }
}
