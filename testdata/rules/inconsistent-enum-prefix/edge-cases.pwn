enum ConditionalState
{
	CONDITIONAL_STATE_NONE,
	CONDITIONAL_STATE_ACTIVE,
	CONDITIONAL_STATE_PAUSED,
#if defined UNKNOWN_STATE
	CONDITIONAL_STATE_FINISHED,
#endif
	STATE_UNKNOWN
}

enum InactiveState
{
	INACTIVE_STATE_NONE,
	INACTIVE_STATE_ACTIVE,
	INACTIVE_STATE_PAUSED,
#if 0
	STATE_UNKNOWN
#endif
}

main()
{
}
